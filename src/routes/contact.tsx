import { createFileRoute } from "@tanstack/react-router";
import { Mail, MapPin, Phone } from "lucide-react";
import { EnquiryForm, PageHero, SectionHeading } from "@/components/shared";

const title = "Request a Quote | Finchnet Ventures";
const description = "Request an internet installation, network consultation, or tailored connectivity quote from Finchnet Ventures.";
export const Route = createFileRoute("/contact")({ head: () => ({ meta: [{ title }, { name: "description", content: description }, { property: "og:title", content: title }, { property: "og:description", content: description }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: "/contact" }] }), component: Page });
const details = [[Phone, "Phone", "To be confirmed"], [Mail, "Email", "To be confirmed"], [MapPin, "Business location", "To be confirmed"]] as const;
function Page() { return <><PageHero eyebrow="Contact Finchnet" title="Let’s Plan Your Connection." text="Tell us about your home, property, or organization and the service you are considering." /><section className="py-20"><div className="site-container grid gap-12 lg:grid-cols-[.75fr_1.25fr]"><div><SectionHeading eyebrow="Request a quote" title="Start With Your Requirements" text="The contact details below are placeholders and will be updated once verified by the client." /><div className="mt-8 grid gap-4">{details.map(([Icon, heading, text]) => <div key={heading} className="flex gap-4 border-t border-border pt-4"><Icon className="size-5 text-blue" /><div><p className="text-xs font-bold uppercase text-muted-foreground">{heading}</p><p className="mt-1 font-bold">{text}</p></div></div>)}</div><div className="network-grid mt-10 h-48 border border-border bg-secondary" aria-label="Location map will be added when the business address is confirmed" /></div><div className="border border-border p-6 md:p-10"><EnquiryForm /></div></div></section></>; }